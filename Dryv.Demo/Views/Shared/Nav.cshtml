@using Dryv.Demo.Nav
@inject NavCollector NavCollector
@{
    var menu = new[]
    {
        Menu.Introduction,
        Menu.ExtendingDryv
    };
    var items = NavCollector.GetNavStructure(this.ViewContext);
}

<ul class="navbar-nav">
    @{
        foreach (var menuItem in menu)
        {
            @Html.Partial("NavItem", items[menuItem])
        }
    }
</ul>